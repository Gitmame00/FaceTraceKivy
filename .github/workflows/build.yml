name: Buildozer Android APK

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4  # ★ v3 -> v4に更新

    - name: Set up Java 11
      uses: actions/setup-java@v4  # ★ v3 -> v4に更新
      with:
        distribution: 'temurin'
        java-version: '11'

    - name: Set up Python 3.8
      uses: actions/setup-python@v5 # ★ v4 -> v5に更新
      with:
        python-version: '3.8'

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          libtool \
          pkg-config \
          zlib1g-dev \
          libncurses5-dev \
          libncursesw5-dev \
          libtinfo5 \
          cmake \
          libffi-dev \
          libssl-dev
        python3.8 -m pip install --upgrade pip
        python3.8 -m pip install buildozer cython==0.29.34

    - name: Build with Buildozer
      run: |
        buildozer -v android debug

    - name: Upload APK
      uses: actions/upload-artifact@v4 # ★★★ ここが最も重要な変更点です ★★★
      with:
        name: facetrace-apk
        path: bin/*.apk